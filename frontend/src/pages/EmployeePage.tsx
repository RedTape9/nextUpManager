import { useParams } from 'react-router-dom';

const EmployeePage = () => {
    //const { id } = useParams();
    // Logik zur Anzeige der Mitarbeiter-Details und zugehörigen Tickets

    return (
        <div>
            <h1>Mitarbeiterseite</h1>
            {/* Links: Liste der Tickets, Rechts: Ticket-Management */}
        </div>
    );
};

export default EmployeePage;